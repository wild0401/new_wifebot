
指令:
所有在線成員，隨機五人一組，分配組成小隊

1.找出"所在伺服器""所有"成員並"隨機"選出x個"在線成員"
2.將所有在線成員"隨機"分配成員(5人一組)
3.送出分配結果訊息
------------------------------------------------------------------
1.找出"所在伺服器""所有"成員並"隨機"選出x個"在線成員"
-列出所在伺服器所有成員>判斷每個成員在線狀態>如果為在線:存放到list(online)中
>再從list(online)裡 "隨機"選出 x 個 存到另外個list(randon_online)中個list(randon_online)中

所在伺服器= ctx.guild
列出所在伺服器所有成員 = ctx.guild.members
判斷每個成員在線狀態 = ctx.guild.mebers
if 成員狀態 == 在線:
    就把該成員加到list(online)中
------------------------------------------------------------------
2.將所有在線成員亂數分配成員(5人一組)
-從在線成員list中隨機挑選五人>存放到小隊list中
-重複以上步驟 直到在線成員被發完
------------------------------------------------------------------
3.送出分配結果訊息
-在DC送出結果訊息
------------------------------------------------------------------